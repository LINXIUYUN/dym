server:
  port: 8080
  address: 127.0.0.1
mybatis:
  mapper-locations: classpath:mybatis/mapper/*.xml
  configuration:
      log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl


spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dym?useUnicode=true&characterEncoding=utf-8&useSSL=true
    username: root
    password: 123456
  thymeleaf:
    prefix: classpath:/templates/
logging:
  level:
    cn.linxiuyun.dym.mapper: debug


